# Training Archive

Place archived YOLO26s training outputs here when needed.

Recommended:

- `training_weight/<version>/best.pt`
- `training_weight/<version>/best.onnx`

Current archive:

- `training_weight/v1/best.pt`
- `training_weight/v1/best.onnx`
